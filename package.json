{
  "name": "angular-hls-video",
  "version": "1.0.1",
  "description": "A simple angular directive that provides support for hls streaming to multiple browsers",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "./node_modules/.bin/browserify src/index.js > angular-hls-video.js --standalone angular-hls-video"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.1"
  },
  "dependencies": {
    "angular": "^1.5.8",
    "hls.js": "^0.6.9"
  }
}
